// GameGraph configuration.
//
// You can optionally hardcode your RAWG API key here so the site "just works"
// without prompting visitors. Get a free key at https://rawg.io/apidocs
//
// WARNING: anything you put here is committed to your public repo and visible
// to anyone. For a personal project on the free tier that's usually fine, but
// if you leave RAWG_KEY empty, GameGraph will instead ask each visitor to paste
// their own key (stored only in their browser). That keeps your key private.
window.GG_CONFIG = {
  RAWG_KEY: "" // e.g. "abcdef0123456789abcdef0123456789"
};
